# yaml-language-server: $schema=../schemas/module.json

name: howdy
type: meson
buildflags:
  - "--prefix=/usr"
  - "--buildtype=release"
sources:
  - type: git
    url: https://github.com/boltgolt/howdy.git
